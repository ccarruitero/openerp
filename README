This is a script for openerp.
(I'm very lazy and want to do everything with a only one line :D, or less it's possible)

First, you will need install: libpq-dev python-dev libxml2-dev libxslt-dev

And you have installed and configured postgres and apache

Then run
sh installer.sh
